# 🚀 ML Service with FastAPI and Redis

Асинхронный ML-сервис, который обрабатывает задачи через очередь **Redis**.  
Сервис поддерживает два типа задач:  
- **Детекция объектов** (`/detect/`)  
- **Прогноз удобрений** (`/fertilizer/`)  

---

## 📁 Структура проекта
./app.py              # Точка входа FastAPI

./models/             # Модели ML

./workers/           # Воркеры и менеджер очередей

./schemas/            # Pydantic-схемы запросов/ответов

./config/             # Настройки Redis, приложения

./utils.py            # Утилиты: проверка Redis, очистка очередей

./requirements.txt

---

## ⚙️ Как всё работает

1. **FastAPI** принимает запрос от пользователя.  
2. Создаётся уникальный `task_id`, и задача помещается в очередь **Redis**.  
3. **Воркеры** слушают свои очереди и выполняют предсказания.  
4. Результат сохраняется в **Redis** по ключу `task_id`.  
5. **FastAPI** ожидает публикации результата и возвращает его клиенту.  
6. Если воркер не получает уведомлений через очередь, он может проверять её периодически (`BLPOP` с таймаутом).

---

## 🔹 Установка

### 1. Клонируем проект
```bash
git clone <repo_url>
cd <project_folder>
```

2. Устанавливаем зависимости:
```bash
pip install -r requirements.txt
```

3. Убедитесь, что у вас установлен и запущен Redis сервер:
```bash
redis-cli ping
# Должно вернуть PONG
```

## 🔹 Запуск сервиса
```bash
uvicorn app:app --reload
```
После запуска сервис будет доступен по адресу:

http://127.0.0.1:8000 (можно заменить в config)

также можно посмотреть авто-сгенерированную доку http://127.0.0.1:8000/docs тут

## 🔹 Эндпоинты 
### 1. Детекция объектов

#### POST /detect/

#### Request JSON
```bash
{
    "image":"https://example.com/image.jpg",
    "metadata":{
        "user":"test",
        "other_info":123
        }
}
```

#### Response JSON
```bash
{
    "task_id":"8a5a8b53-924d-4938-8c8c-f89b8f10fe9f",
    "result":{
        "objects":["example_object"],
        "data":{
            "image":"https://example.com/image.jpg",
            "metadata":{
                "user":"test",
                "other_info":123
            }
        }
    }
}
```

### 2. Рекомендации по удобрениям

#### POST /fertilizer/

#### Request JSON
```bash
{
    "field_id": 2,
    "soil_data":{
        "user":"test",
        "other_info":123
    }
}
```

#### Response JSON
```bash
{
    "task_id":"e0831bfa-a3fe-4f77-b65f-8d78f00f89dc",
    "result":{
        "recommendations":["fertilizer"],
        "data":{
            "field_id":2,
            "soil_data":{
                "user":"test",
                "other_info":123
            }
        }
    }
}
```


## Примечания
• Воркеры работают асинхронно и могут обрабатывать задачи одновременно.

• Redis должен быть запущен, иначе сервис завершит работу с ошибкой.

• Используются Pydantic-схемы для валидации данных.

## Примеры запросов через curl
```bash
curl -X POST http://127.0.0.1:8000/fertilizer/ -H "Content-Type: application/json" -d '{"field_id": 2,"soil_data":{"user":"test","other_info":123}}'
```

```bash
curl -X POST http://127.0.0.1:8000/detect/ -H "Content-Type: application/json" -d '{"image":"https://example.com/image.jpg","metadata":{"user":"test","other_info":123}}'
```
